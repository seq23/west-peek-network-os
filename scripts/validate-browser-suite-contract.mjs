import fs from "node:fs";const c=JSON.parse(fs.readFileSync("_browser_suite_contract.json","utf8"));const b=c.browser_suite;if(!b?.command||!Array.isArray(b.projects)||!b.projects.length)throw new Error("invalid browser suite contract");if(b.required_failed!==0)throw new Error("required_failed must equal 0");if(b.expected_collected!==undefined&&(!Number.isInteger(b.expected_collected)||b.expected_collected<1))throw new Error("expected_collected invalid");if(b.count_policy===undefined&&b.expected_collected===undefined)throw new Error("count policy missing");if(!Array.isArray(b.registered_skips))throw new Error("registered_skips missing");console.log("BROWSER SUITE CONTRACT: PASS",JSON.stringify({command:b.command,projects:b.projects,expected_collected:b.expected_collected??b.count_policy,registered_skips:b.registered_skips.length}));
