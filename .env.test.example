# GENERATED_BY=generic-testing-architecture-capability-installer
NODE_ENV=test
PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000
TEST_RUNTIME=container
# Add provider fixture variables only after the repo implements matching adapters.
# Add only safe deterministic test values. Never add production secrets.
APP_ENV=test
AUTH_PROVIDER=test
DATA_PROVIDER=fixture
GMAIL_PROVIDER=fixture
SHEETS_PROVIDER=local-adapter
