<!-- GENERATED_BY=generic-testing-architecture-capability-installer -->
# Test Fixtures

Store deterministic, non-secret, non-production fixtures here. Provider-specific
fixtures must match production contracts without being represented as live proof.
